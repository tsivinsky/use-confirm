{
  "name": "use-confirm",
  "description": "asynchronous confirm function for React",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/use-confirm.cjs",
  "module": "./dist/use-confirm.esm.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./dist",
    "build:dev": "yarn clean && NODE_ENV=development node build.js",
    "build:prod": "yarn clean && NODE_ENV=production node build.js",
    "prepublishOnly": "yarn build:prod"
  },
  "devDependencies": {
    "@types/react": "^17.0.43",
    "esbuild": "^0.14.30",
    "esbuild-plugin-d.ts": "^1.1.0",
    "react": "^18.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  },
  "peerDependencies": {
    "react": "^16.8.0 || ^17 || ^18"
  },
  "exports": {
    ".": {
      "require": "./dist/use-confirm.cjs",
      "import": "./dist/use-confirm.esm.js"
    }
  },
  "files": [
    "dist"
  ]
}
